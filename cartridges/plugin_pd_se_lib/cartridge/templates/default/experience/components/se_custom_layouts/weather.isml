<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    assets.addCss('/css/experience/components/commerceLayouts/layout.css');
</isscript>
<!-- Weather Settings: ${pdict.weatherTemperature}°C - ${pdict.city} -->
<isif condition=${pdict.displayLayout}>
<div class="weather">    
    <div class="row mx-n2">        
        <isprint value="${pdict.regions.weather.setClassName("region col-12").render()}" encoding="off"/>
    </div>
</div>
</isif>
